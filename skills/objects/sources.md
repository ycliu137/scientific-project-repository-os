# Type skill — sources

Read when creating or editing `S_*`. The folder `objects/sources/` is created on the first create.

- Paper, dataset, URL, or internal note used as provenance.
- Do not dump an entire bibliography. Create `S_*` when a claim or hypothesis cites it.
- Parent path: `{slug}/objects/sources`.
