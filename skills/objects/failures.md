# Type skill — failures

Read when creating or editing `F_*`. The folder `objects/failures/` is created on the first create.

- Falsification, protocol break, or method that must not be silently retried.
- Keep the object. Link the hypothesis/experiment that failed.
- Parent path: `{slug}/objects/failures`.
