# Scratch notes only — NOT the long-term project record.

Promote process / params / important results to `tex_docs/` via:
`python3 tools/lab.py promote <lab_id> --tex tex_docs/labs/<topic>`
Then this lab may be discarded (`skills/backend-lab.md`).
