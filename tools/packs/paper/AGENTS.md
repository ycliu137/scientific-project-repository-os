# AGENTS.md — Paper pack

1. Read `SKILL.md` then `MAP.md`. This repo is the **writing entry**.
2. `python3 tools/bridge.py status` before claiming facts about code or data.
3. Default sibling is **pipeline** (objects, TeX notes). Source = package tests.
4. Do not copy `src/`, `objects/`, or datasets into this repo.
5. Done = paper Change Report + sibling integrity if those repos changed.
