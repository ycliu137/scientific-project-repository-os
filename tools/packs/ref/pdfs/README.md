# PDFs (local only)

All reference PDFs live here, named by **article title slug**:

```text
pdfs/<Title_Slug>.pdf
```

Same slug as `papers/<Title_Slug>/`. Merge main + appendix into this one file.

**Gitignored:** `pdfs/*.pdf` are not committed. Keep this README.
