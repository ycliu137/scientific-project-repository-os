# AGENTS.md — Ref pack

1. This is the **reference literature** corpus, not the default work entry.
2. Prefer the **pipeline** sibling for ingest, reading, and linking to `S_*` objects.
3. Prefer the **paper** sibling when adding bibliography citations to the manuscript.
4. Every paper: `papers/<Title_Slug>/meta.yaml` + `pdfs/<Title_Slug>.pdf` (title-slug name; PDF gitignored).
5. Do not commit PDFs. Do not import pipeline. Do not put the publicable source package here.
